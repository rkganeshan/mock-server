{
  "name": "soar-mock-server",
  "version": "1.0.0",
  "description": "Mock server to host user details and transactions, cards, expenses data.",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev":"json-server db.json --watch --port 8080 --delay 3000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.7",
    "json-server": "^0.17.4"
  }
}
